library(shiny)
shinyServer(function(input, output) {
  
  #Aggregating similar positions
  LB <- c('LB', 'MLB', 'ILB', 'OLB')
  S <- c('S', 'FS', 'SS')
  OL <- c('OL', 'C', 'G', 'NT', 'T')
  
  
  
})